# 일본어 마스 — 작업 규칙

## 구현이 끝난 설계 문서는 지운다

`docs/superpowers/` 아래 스펙·계획 문서는 **구현이 끝나면 그 자리에서 지우고 커밋한다.**
작업하는 동안만 쓰는 문서라, 남겨 두면 코드와 어긋난 설명이 저장소에 쌓인다.

내용은 그것을 만든 커밋에 그대로 남으므로 언제든 꺼내 볼 수 있다:

```
git log --diff-filter=D --name-only -- docs/superpowers/   # 언제 무엇이 지워졌는지
git show <커밋>:docs/superpowers/specs/<파일>              # 그 시점 내용
```

지우지 말라는 뜻이 아니라 **문서를 건너뛰라는 뜻도 아니다.** 설계는 그대로 먼저 쓴다 —
구현이 끝난 뒤에 치우는 것만 다르다.

지우기 전에 **스펙에만 있고 README에 없는 결정을 README로 옮긴다.** 기능 설명과 「왜
이렇게 했나」는 README 하나로 관리한다.
