# 일본어 마스 (日本語マス)

한국어 사용자를 위한 일본어 쓰기 연습 앱. 안드로이드 네이티브 (Kotlin + Jetpack Compose).

## 화면 이동

왼쪽 위 햄버거(☰)를 누르거나 화면 왼쪽 가장자리를 쓸면 메뉴가 열립니다.
연습 기능은 **범위 고르기 → 연습** 두 단계이고, 뒤로가기는 한 단계씩 올라갑니다.

```
오늘  →  가나 맞추기 (서체 고르기)  →  가나 맞추기 (연습)
     ←  뒤로                       ←  뒤로
```

메뉴는 `오늘` · `가나 맞추기` · `단어 맞추기` · `오답 노트` — `찾기` · `설정` 순입니다.
범위는 가나 맞추기가 `서체`, 단어 맞추기가 `JLPT 등급 × (한자 | 단어 분류)`입니다.
찾기와 설정은 연습이 아니라 범위 고르기 없이 한 단계입니다.

오늘 화면에서 뒤로 가면 앱이 닫힙니다. 오답 노트는 그 자체가 목록이라 한 단계입니다.

## 담긴 기능

1. **가나 맞추기** — 히라가나·가타카나 중 하나를 고르고 들어가, 안에서 익히는 방식을
   바꿔 가며 한다. 청음·탁음·요음을 나누지 않고 104자를 한 통에 두고 약한 글자부터
   섞어 낸다. 앞의 세 방식이 같은 기록을 쓰므로 어느 쪽으로 익혀도 복습 일정은 한 벌이다.

   | 방식 | 하는 것 | 채점 |
   |---|---|---|
   | 써보기 | 회색 안내 글자 위에 덧쓴다. 진하기를 0까지 줄이면 빈 칸 쓰기가 된다 | 없음(연습) |
   | 로마자 | 글자를 보고 로마자를 친다 | 자동 |
   | 듣고 쓰기 | 발음만 듣고 빈 칸에 쓴 뒤 정답을 붉게 겹쳐 본다 | 모양 점수 + 직접 |
   | 스피드 | 60초 동안 뜨는 글자의 로마자를 친다 | 자동. **기록엔 안 남는다** |

   로마자는 헵번식과 일본식을 둘 다 받는다 (`shi`/`si`, `tsu`/`tu`, `ん`은 `n`·`nn`).
   써보기에는 안내 글자 진하기 슬라이더와 십자 안내선 토글이 붙는다.

   스피드만 성격이 다르다 — 익히는 자리가 아니라 얼마나 붙었는지 재는 자리다. 맞는 답을
   치는 순간 확인 없이 넘어가고(엔터와 「패스」는 넘기기), 60초가 지나면 점수가 뜬다.
   복습 기록은 건드리지 않고 서체별 최고 점수 하나만 남는다. 1분에 수십 장을 치는 놀이를
   복습 일정에 흘리면 오늘 처음 본 글자가 한 판에 익힘까지 올라간다.

2. **단어 맞추기** — 단어와 한자를 떠올린 뒤 직접 채점한다. 보기가 없어 답을 역추적할
   수 없다. 묻는 방향을 화면 위에서 골라 같은 데이터를 세 가지로 돌려 볼 수 있다.

   | 방향 | 묻는 것 | 기록 |
   |---|---|---|
   | 뜻 | 글자를 보고 뜻·읽기 | 기본 |
   | 듣기 | 소리만 듣고 무슨 말인지 | 따로 |
   | 빈칸 | 예문에서 지운 단어 (단어만) | 뜻과 공유 |

   보고 아는 것과 듣고 아는 것은 다른 능력이라 방향마다 복습 일정을 따로 센다.
   빈칸만은 뜻을 묻는 것과 같은 지식이라 기록을 함께 쓴다 — 문맥 안에서 한 번 더
   꺼내 보는 복습이다. 한자에는 문장 예문이 없어 빈칸 방향이 없다.

   정답면은 단어가 읽기·예문, 한자가 음독·훈독·구성·예시 단어를 한 줄씩 놓고 줄마다
   재생 단추가 따로 붙는다. 구성은 조각으로 뜻이 설명되는 회의자에만 뜬다.
   맨 아래에는 이어보기 한 줄이 붙는다 — 한자에는 **그 글자가 든 단어**(`電` → `電車 전철`)를
   여섯 개까지, 단어에는 **표기에 든 한자**(`電車` → `電 번개 전`)를 칩으로 깐다. 칩을 누르면
   발음만 나고 그 카드로 건너가지는 않는다. 건너뛰면 돌던 묶음이 날아간다.
   히라가나뿐인 단어는 칩이 없어 줄 자체가 안 뜬다.
   듣기 방향은 소리가 잘 안 들리거나 이어폰이 없을 때 '가나 보기'를 눌러 읽기를
   글자로 확인한다.

3. **오답 노트** — 오늘 복습할 카드와 틀린 카드를 모아 본다. `오늘 복습` · `자주 틀림` ·
   `틀린 적 있음` · `배운 카드 전체` 넷으로 거르고 기본은 `오늘 복습`이다 — 홈의 첫 단추가
   여기로 온다. 방향이 다른 카드는 `· 듣기` 처럼 꼬리표가 붙는다.
   연습 정답면은 단어 맞추기와 **같은 줄들**이라 음독·훈독·예문이 줄마다 따로 소리 나고
   이어보기 칩도 그대로 붙는다. 목록 줄은 좁아서 지금처럼 눌러 한 번 재생한다.
   위에 익힘·복습 대기·자주 틀림 숫자가 서고, 아래에 최근 14일 기록과
   카드별 초기화·전체 기록 지우기가 있다.

4. **찾기** — 한자와 단어를 표기·읽기·뜻 아무거나로 찾는다. 연습이 아니라 사전이라
   범위를 안 고른다. 앞에서부터 맞는 것을 먼저 올린다 — '일'로 찾으면 '날 일'이
   '일요일'보다 앞에 온다. 한 번에 50줄까지 보여주고, 줄마다 지금 익힘 단계가 붙는다.

**오늘** 화면은 오늘 복습할 카드 수를 맨 위에 홀로 얹고, 그 아래 최근 7일 연속 기록과
전체 익힘 비율을 둔다. 복습할 카드가 있으면 첫 단추가 그 복습으로 간다 — 머리의 숫자와
단추가 같은 것을 가리킨다. 다 따라잡았으면 가나 연습으로 보낸다.
상단 ⓘ는 복습이 어떻게 도는지 설명한다.

복습은 맞히면 1 → 2 → 4 → 8 → 16 → 32 → 64일 뒤에 다시 나오고, 7단계에 닿으면
'익힘'으로 넘어가 복습 목록에서 빠진다. 틀리면 두 단계 떨어져 그날 다시 나오며
그 자리에서 4~8장 뒤에 한 번 더 묻는다. 간격이 고정이면 답이 아니라 순서를 외우게
되어 매번 이 범위에서 뽑는다.

8일에서 끊지 않고 두 달까지 끌고 가는 이유는, 거기서 멈추면 카드가 영영 다시 안 나와
한 달 뒤에는 잊었는데 앱은 익혔다고 표시하는 상태가 되기 때문이다.

**오늘 통과한 카드는 그날 다시 나오지 않는다.** 「한 바퀴 더」에서 다시 뜨면 점수가
계속 올라 오늘 처음 본 글자가 일곱 바퀴에 익힘이 된다. 틀린 카드는 복습일이 오늘로
남으므로 여기 걸리지 않는다 — 틀린 건 그날 다시 물어야 한다.

묶음 끝이라 사이에 낄 카드가 없으면 **이미 배운 카드 중 본 지 오래된 것**을 먼저
끌어와 채우고, 그런 게 없으면 새 카드로라도 자리를 띄운다. 자리 채우기로 끌려온 새
카드는 `맛보기`로 표시되고 채점하지 않는다 — 배운 적 없는 글자가 오답 노트에 뜨면
안 된다.

묶음은 정해진 크기의 두 배까지만 길어진다. 상한에 닿으면 틀려도 다시 끼우지 않고
묶음을 끝낸다 — 맞힐 때까지 붙잡아 두면 그날 학습이 끝나지 않는다. 틀린 카드는 이미
기록됐으므로 다음 묶음에서 제일 먼저 나온다.

## 설정

| 항목 | 하는 일 |
|---|---|
| 화면 | `기기 설정` · `밝게` · `어둡게`. 기기가 어두워도 앱만 밝게 고정할 수 있다 |
| 한 묶음 구성 | 새 카드(기본 5)와 복습 카드(기본 10)를 `0~30` 슬라이더로 따로 고른다. 묶음 크기는 둘의 합이고 둘 다 0으로 둘 수는 없다 |
| 가나 연습 | 끄면 메뉴·복습·홈의 익힘 비율에서 가나 208장이 통째로 빠진다. 기록은 남으므로 다시 켜면 돌아온다 |
| 소리 없이 연습 | 자동 재생만 끈다. 「다시 듣기」·「발음」을 누르면 그대로 난다 |
| 기록 | 학습 기록을 파일 한 장으로 내보내고 되돌린다. 설정은 안 담긴다 |

새 카드 수를 따로 두는 이유는, 몫을 안 정하면 밀린 복습이 묶음을 통째로 채워서 새
단어가 한 장도 안 나오기 때문이다. 한쪽이 몫을 다 못 채우면 남은 자리는 다른 쪽이
받지만, **새 카드 0장은 예약 자리가 0이라는 뜻이 아니라 아예 안 낸다는 뜻이다** —
복습을 다 따라잡은 날에도 남은 자리가 새 카드로 넘어가지 않는다.

「소리 없이 연습」은 듣고 쓰기가 화면에 들어가는 순간 자동으로 소리를 내는 것만 막는다.
그때는 문제를 소리 대신 로마자로 내고, 상단 문구도 "로마자를 보고 칸에 써 보세요"로
바뀐다. 듣고 쓰기 화면 안의 「로마자 보기」 칩이 같은 설정을 바로 켜고 끈다.

## 담긴 데이터

| | N5 | N4 | N3 | N2 | 합계 |
|---|---:|---:|---:|---:|---:|
| 한자 | 80 | 167 | 397 | 387 | 1,031 |
| 단어 | 710 | 653 | 2,062 | 1,746 | 5,171 |

단어 분류는 `사물` · `동사` · `형용사` · `부사` · `사람` · `장소` · `시간` · `자연` ·
`음식` · `인사` 열 가지이고, 등급마다 실제로 쓰이는 분류만 목록에 뜬다.

가나는 104행이고 히라가나·가타카나를 따로 익히므로 카드는 208장입니다.
모든 단어에 예문(일본어·읽기·한국어 뜻)이 붙어 있습니다.

위 표는 **글자·단어의 수**입니다(카드 6,410장). 단어와 한자는 묻는 방향마다 복습 일정이
따로 가므로 실제 복습 대상은 12,612개입니다 (`cardIds()` 대 `studyIds()`).

데이터는 소스가 아니라 `app/src/main/resources/` 의 TSV 표에 들어 있습니다.
등급을 다 채우면 수천 줄이 되는데, 그만한 `listOf`는 JVM 메서드 64KB 한도에 걸리고
빌드도 느려집니다. 표로 두면 앱과 JVM 단위 테스트가 같은 파일을 읽어서 `Context`를
넘길 일도, 테스트에 Robolectric을 끌어올 일도 없습니다.

| 파일 | 열 |
|---|---|
| `kanji.tsv` | 한자 · 뜻·음 · 음독 · 훈독 · 예시 단어 · 예시 읽기 · 예시 뜻 · 등급 · 구성 |
| `vocab.tsv` | 표기 · 읽기 · 뜻 · 분류 · 등급 · 예문 · 예문 읽기 · 예문 뜻 |

줄을 추가하면 그대로 늘어나고, `DataTest`가 열쇠 중복·가나 표기·예문 누락·빈칸 뚫림을
바로 잡아 줍니다 (등급별 개수도 고정하고 있으니 함께 고쳐야 합니다).

## 빌드 방법

### Android Studio (권장)

1. [developer.android.com/studio](https://developer.android.com/studio) 에서 설치
2. 첫 실행 시 SDK 설치 마법사를 기본값으로 진행
3. `File → Open` 에서 이 폴더(`NihongoMasu`)를 선택
4. Gradle 동기화가 끝나면 상단 ▶ 실행

### 터미널

래퍼가 들어 있어 그대로 돌아갑니다.

```
./gradlew assembleDebug      # 디버그 APK
./gradlew testDebugUnitTest  # 단위 테스트 77개
```

**JDK는 `gradle.properties`의 `org.gradle.java.home`에 21로 못 박혀 있습니다.**
Gradle 8.9가 JDK 22까지만 도는데 이 기기 기본 java가 25라, 못 박지 않으면 설정 단계에서
죽습니다. 다른 기기에서 받았다면 그 줄의 경로를 자기 JDK 21 자리로 고치세요.
(안드로이드 스튜디오는 `.idea/gradle.xml`의 `gradleJvm=jbr-21`로 따로 21을 씁니다.)

최소 안드로이드는 8.0(API 26), 컴파일·타깃은 API 35입니다.

### APK 파일 뽑기

- 디버그용: `Build → Build Bundle(s) / APK(s) → Build APK(s)`
  → `app/build/outputs/apk/debug/app-debug.apk`
- 배포용 서명 APK: `Build → Generated Signed Bundle / APK`

디버그 APK도 폰에 바로 설치됩니다. 설정에서 "출처를 알 수 없는 앱 설치"만 허용하면 됩니다.

## 구성

| 경로 | 역할 |
|---|---|
| `src/main/resources/*.tsv` | 학습 데이터 표 |
| `data/Kana.kt` `Kanji.kt` `Vocab.kt` | 데이터 타입과 표 읽기 |
| `data/Quiz.kt` | 카드 종류(단어·한자)와 묻는 방향(뜻·듣기·빈칸) |
| `data/Srs.kt` | 복습 간격·묶음 구성 계산. 안드로이드 의존성 없는 순수 함수 |
| `data/Store.kt` | SharedPreferences + JSON 저장, 설정 |
| `data/Backup.kt` | 기록을 파일 한 장으로 내보내고 되읽기 |
| `data/RomajiCheck.kt` | 로마자 채점 (헵번식·일본식 둘 다) |
| `data/Search.kt` | 찾기. 표기·읽기·뜻 어디로든 |
| `draw/ShapeCompare.kt` | 손글씨와 정답 글자의 모양 비교. 순수 계산 |
| `draw/StrokeCanvas.kt` | 필기 캔버스, 안내 글자, 정답 겹쳐 보기 |
| `tts/Speaker.kt` | 일본어 발음 (안드로이드 내장 TTS) |
| `ui/App.kt` | 드로어, 상단 바, 뒤로가기 스택, 오늘 화면 |
| `ui/Face.kt` | 정답면. 읽기 줄들과 한자↔단어 이어보기. 두 화면이 함께 쓴다 |
| `ui/Parts.kt` | 공용 부품 — 묶음 진행, 되돌리기, 한 바퀴 마침, 애니메이션 |
| `ui/Theme.kt` | 팔레트·폰트·테마 모드 |
| `ui/` | 화면들. 연습 기능마다 `XxxFlow` 안에 목록과 연습이 들어 있다 |
| `src/test/` | 단위 테스트 77개 (JVM에서 바로 실행) |

`Srs.kt` `ShapeCompare.kt` `RomajiCheck.kt` `Search.kt` `Backup.kt`는 일부러 안드로이드
API를 쓰지 않았습니다. 덕분에 기기 없이도 JVM에서 바로 테스트할 수 있고, `app/src/test/`가 그 값을
고정해 둡니다.

| 테스트 | 고정하는 것 |
|---|---|
| `SrsTest` (29) | 복습 간격, 채점 등급 넷, 묶음 구성, 오답 재삽입, 같은 날 반복 차단 |
| `DataTest` (17) | 등급별 개수, 열쇠 중복, 가나 표기, 예문 누락, 빈칸 뚫림, 한자↔단어 색인 |
| `ShapeCompareTest` (8) | 모양 점수의 경계값 |
| `RomajiTest` (8) | 헵번식·일본식 표기, 대소문자·공백 |
| `SearchTest` (7) | 찾기 순위와 빈 물음 |
| `GlyphFitTest` (3) | 요음처럼 두 글자인 카드가 칸을 안 넘는지 |
| `BackupTest` (5) | 내보내기 왕복, 남의 파일 거절, 사다리 밖 점수 자르기 |

## 모양 비교는 어떻게 채점하나

겹치는 픽셀을 그냥 세면 손글씨는 거의 0점이 나옵니다. 사람이 쓴 선은 정답과 몇 픽셀씩
어긋나기 마련이라서요. 그래서 양쪽을 조금 부풀린 뒤 두 방향으로 나눠 봅니다.

- **덮음(coverage)** — 정답 획 중 내가 지나간 비율 → 빠뜨린 획을 잡아낸다
- **정확도(accuracy)** — 내 필기 중 정답 위에 놓인 비율 → 삐져나간 선을 잡아낸다

한쪽만 높아도 좋은 글씨가 아닙니다. 아무것도 안 쓰면 정확도가 100이 되고, 칸을 새까맣게
칠하면 덮음이 100이 됩니다. 그래서 두 값을 함께 보는 F-베타 평균(베타=2)으로 총점을 내고,
획을 빼먹는 쪽에 네 배 무게를 줬습니다. 칸을 절반 넘게 칠하면 20점으로 제한합니다.

이 경계값들은 `ShapeCompareTest`가 고정합니다: 동일 100점, 2칸 밀림 100점,
두 획 중 하나 누락은 "획이 빠졌어요" 안내와 함께 크게 감점, 전체 칠하기 20점 상한.

## 일본어 발음이 안 들릴 때

기기에 일본어 음성 데이터가 없는 경우입니다.
`설정 → 시스템 → 언어 및 입력 → 음성 출력`에서 일본어를 받으면 들립니다.
없는 동안에는 가나 듣고 쓰기에 로마자가 대신 표시되고,
단어 맞추기의 듣기 방향은 '가나 보기'로 읽기를 볼 수 있습니다.
「소리 없이 연습」을 켜 둔 사람에게는 음성을 받으라는 안내를 띄우지 않습니다.

## 기록 저장

기록은 기기 내부(SharedPreferences)에 저장되고 안드로이드 자동 백업 대상에 포함됩니다.
자동 백업은 같은 구글 계정으로 복원할 때만 돌아오므로, **설정 → 기록**에서 파일 한 장으로
직접 내보낼 수 있습니다. 폰을 바꾸기 전에 한 번 내보내 두면 됩니다.

내보낸 파일은 저장에 쓰는 것과 같은 JSON이고, 카드별 기록과 최근 학습한 날만 담습니다
(설정은 안 담습니다). 가져오면 지금 기록은 **통째로 대체**됩니다 — 합치는 것이 아니라
그 파일 시점으로 되돌아갑니다. 읽을 수 없는 파일은 아무것도 바꾸지 않고 거절합니다.
저장 자리는 안드로이드 파일 선택기가 맡아서, 앱은 저장소 권한을 하나도 요구하지 않습니다.

앱을 삭제하면 기기 안의 기록은 함께 사라집니다. 설정도 학습 기록과 같은 파일에 들어갑니다 —
설정 하나를 고쳐도 파일 전체가 다시 쓰이는 구조라, 슬라이더는 손을 뗄 때만 저장합니다.

## 아직 없는 것

- **복습 알림** — 간격 반복은 매일 앱을 열어야 뜻이 있는데, 여는 계기가 사용자 기억뿐입니다.
  `WorkManager` + `POST_NOTIFICATIONS`가 필요합니다.
- **획순** — `ShapeCompare`는 모양만 견주므로 획 순서와 방향이 틀려도 통과합니다.
  글자마다 획순 데이터가 있어야 고칠 수 있습니다.
- **빈칸의 활용형** — 어간으로 지우므로 예문 안 다른 낱말에 같은 글자가 있으면 거기까지
  뚫립니다 (`Word.clozed`의 `ponytail:` 주석 참고).
- **N4·N3 전체 범위** — 공식 목록이 아니라 자주 쓰이는 것만 골라 담았습니다. `DataTest`가
  잡는 것은 형식이라(읽기가 가나인지, 예문이 비지 않았는지) 뜻이 맞는지는 사람이 봐야 합니다.

## 라이선스와 데이터 출처

코드는 **MIT**, 학습 데이터(`app/src/main/resources/*.tsv`)는 **CC BY-SA 4.0**입니다.
데이터만 라이선스가 다른 것은 바탕이 된 KANJIDIC2가 CC BY-SA이기 때문입니다 —
그 두 파일을 가져다 쓰려면 출처를 밝히고 같은 라이선스를 유지해야 합니다.
코드 쪽에는 그런 제약이 없습니다.

| 무엇 | 출처 | 라이선스 |
|---|---|---|
| 한자 음독·훈독 | [KANJIDIC2](https://www.edrdg.org/wiki/index.php/KANJIDIC_Project) (EDRDG) | CC BY-SA 4.0 |
| 일본어 예문 | [Tatoeba Project](https://tatoeba.org/) | CC BY 2.0 FR |
| JLPT 단어 등급 | [open-anki-jlpt-decks](https://github.com/jamsinclair/open-anki-jlpt-decks) | MIT |
| 한자 한글 훈음 | [libhangul](https://github.com/libhangul/libhangul) `hanja.txt` | BSD 3-Clause |

한국어 번역과 뜻풀이, 급수 재배치는 이 저장소에서 한 것입니다. 자세한 저작권 표기는
[NOTICE](NOTICE)에 있습니다.

간격 반복이라는 방식 자체는 [Anki](https://apps.ankiweb.net/)를 비롯한 여러 앱에서
가져온 것이지만, 코드는 어느 쪽에서도 옮겨오지 않았습니다.
